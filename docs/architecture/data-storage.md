# 数据存储层

## 双层存储架构

```
┌─────────────────────────────────────────────────────────────────────┐
│  层 1: JSON 配置层 (文件系统)                                       │
│  ─────────────────────                                             │
│  providers.json  → 提供商注册信息                                   │
│  strategies.json → 路由策略配置                                     │
│  api_keys.json   → API Key 列表                                    │
│  settings.json   → 系统设置                                        │
│                                                                     │
│  特点: 人类可读、Git 版本管理、热重载                                │
├─────────────────────────────────────────────────────────────────────┤
│  层 2: 运行时数据层 (进程内)                                        │
│  ─────────────────────                                             │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  内存 (moka + dashmap)                                       │  │
│  │  ├─ 熔断器状态 (provider → health_score)                      │  │
│  │  ├─ 速率限制计数器 (key → count + TTL)                        │  │
│  │  ├─ 响应缓存 (request_hash → response, 自动 TTL)              │  │
│  │  └─ 统计计数器 (总请求数、成功数、失败数)                       │  │
│  │  优势: 零依赖、亚微秒级访问、进程启动即初始化                    │  │
│  └──────────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  DuckDB (嵌入式列式数据库)                                    │  │
│  │  ├─ 调用日志 (每次请求的完整记录)                               │  │
│  │  ├─ 成本统计 (按提供商/模型/时间维度聚合)                       │  │
│  │  ├─ 模型画像 (延迟 P50/P95、成功率、质量评分)                  │  │
│  │  └─ 延迟分布直方图                                            │  │
│  │  优势: 列式分析引擎、单文件持久化、ACID 事务                     │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

## 存储目录

```
data/
├── config/
│   ├── providers.json
│   ├── strategies.json
│   ├── api_keys.json
│   └── settings.json
├── smart_router.duckdb
└── archive/
    └── smart_router_2026-08.duckdb
```